const DATABASE_NAME = "@gameplay";

//@app:colletion nome para padronizar a coleção 

const COLLECTION_USER = `${DATABASE_NAME}:user`;
const COLLECTION_APPOINTMENT = `${DATABASE_NAME}:appointment`;

export {
    COLLECTION_USER,
    COLLECTION_APPOINTMENT
}